# Copy this file to ".env" (no .example) and paste your real key in.
# The .env file is git-ignored — it never leaves the laptop.
OPENAI_API_KEY=sk-replace-with-your-key

# Optional. Defaults to 3000.
PORT=3000
